`NN_Spiral_Multiclass_torch.ipynb`는 일반적으로 **Spiral Dataset(나선형 데이터)** 을 이용하여 다중 분류(Multi-class Classification)를 수행하는 신경망 실습입니다. 특히 활성화 함수(ReLU, Sigmoid, Tanh, LeakyReLU 등)의 차이가 학습 성능에 큰 영향을 주는 대표적인 예제입니다.


# 실습문제 1. ReLU 활성화 함수 제거하기
현재 모델에서 사용 중인 ReLU 활성화 함수를 제거하고 학습을 수행하시오.

### 수정 전
```
x = self.fc1(x)
x = self.relu(x)
x = self.fc2(x)
```

### 수정 후
```
x = self.fc1(x)
x = self.fc2(x)
```

## 확인 사항
1. 최종 정확도는 얼마인가?
2. Loss 감소 속도는 어떻게 변하는가?
3. 왜 성능이 감소하는가?



# 실습문제 2. ReLU → Sigmoid 변경
ReLU를 Sigmoid로 변경하여 학습하시오.

```
self.activation = nn.Sigmoid()
```
## 결과표

| 항목          | ReLU | Sigmoid |
| ------------- | ------- | ----------- |
| 최종 Loss     | ?     | ?          |
| 최종 Accuracy | ?    | ?       |
| 학습속도        | ?     | ?       |

## 질문
1. 어느 함수가 더 빨리 수렴하는가?
2. Sigmoid에서 기울기 소실(Vanishing Gradient)이 발생하는 이유는?



# 실습문제 3. ReLU → Tanh 변경
활성화 함수를 Tanh로 변경하시오.

```
self.activation = nn.Tanh()
```
## 결과 기록

| Epoch | Loss |
| -------- | ------ |
| 100    |         |
| 500    |        |
| 1000  |        |

## 질문
1. Tanh의 출력 범위는?
2. Sigmoid보다 좋은 점은?



# 실습문제 4. LeakyReLU 적용
LeakyReLU를 적용하시오.

```
self.activation = nn.LeakyReLU(
    negative_slope=0.01
)
```

## 질문
1. Dead ReLU 문제가 해결되는가?
2. ReLU보다 정확도가 향상되는가?


# 실습문제 5. ELU 적용
ELU 활성화 함수를 사용하시오.

```
self.activation = nn.ELU(alpha=1.0)
```

## 결과표
| 활성화 함수    | 정확도 |
| ----------------- | -------- |
| ReLU            |           |
| LeakyReLU  |           |
| ELU             |           |

## 질문
ELU가 음수 영역을 사용하는 이유는?



# 실습문제 6. 활성화 함수 성능 비교
다음 활성화 함수를 모두 적용하여 결과를 비교하시오.

```
nn.ReLU()
nn.Sigmoid()
nn.Tanh()
nn.LeakyReLU()
nn.ELU()
```

## 결과표

| 함수        | Loss | Accuracy |
| ----------- | ------ | ------------ |
| Sigmoid   |       |                |
| Tanh      |         |                |
| ReLU      |       |                |
| LeakyReLU |      |            |
| ELU       |        |               |

## 질문
1. 가장 높은 정확도를 보인 함수는?
2. 가장 빠르게 수렴한 함수는?
3. 가장 낮은 Loss를 보인 함수는?


# 실습문제 7. 은닉층 수 증가
현재 은닉층을 1개에서 2개로 증가시키고 ReLU를 적용하시오.

```
self.fc1 = nn.Linear(2, 64)
self.fc2 = nn.Linear(64, 64)
self.fc3 = nn.Linear(64, 3)
```

```
x = self.relu(self.fc1(x))
x = self.relu(self.fc2(x))
x = self.fc3(x)
```

## 질문
1. 활성화 함수가 없다면 어떻게 되는가?
2. 활성화 함수가 여러 층에서 필요한 이유는?



# 실습문제 8. 활성화 함수 시각화

## 문제
다음 코드를 실행하여 활성화 함수를 시각화하시오.

```
import torch
import matplotlib.pyplot as plt

x = torch.linspace(-5, 5, 100)

relu = torch.relu(x)
sigmoid = torch.sigmoid(x)
tanh = torch.tanh(x)

plt.plot(x, relu, label="ReLU")
plt.plot(x, sigmoid, label="Sigmoid")
plt.plot(x, tanh, label="Tanh")

plt.legend()
plt.grid()
plt.show()
```

## 질문
1. ReLU의 기울기는 어디서 0인가?
2. Sigmoid는 왜 포화(Saturation)가 발생하는가?
3. Tanh가 Sigmoid보다 좋은 이유는?



